# Render Build & Deploy Script
# This file tells Render how to build and start your app

# Build the frontend (creates dist/ folder)
npm run build

# Server will be started with the "start" script from package.json
